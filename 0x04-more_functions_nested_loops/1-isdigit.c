#include "main.h"

/**
 * _isdigit -  function that checks for a digit (0 through 9).
 *
 * @c: check input
 *
 * Return: 1 if c digit 0 otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
