#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 *
 * @a : control first argument
 * @b : control second argument
 *
 * Return: The sum a and b : a+ b
 */
int add(int a, int b)
{
	return (a+ b);
}
