#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Print all single numbers
 *
 * Return: (0)
*/

int main(void)
{
	int digit = 0;

	while (int <= 9)
	{
		printf("%i", digit);
		digit++;
	}
	putchar('\n');
	return (0);
}
